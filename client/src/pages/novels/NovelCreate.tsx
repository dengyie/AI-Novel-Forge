import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";

export default function NovelCreate() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>创建小说</CardTitle>
        <CardDescription>新建小说页占位。</CardDescription>
      </CardHeader>
      <CardContent>后续将在此接入完整的表单校验与创建流程。</CardContent>
    </Card>
  );
}

inject-workspace-packages=true
# pnpm deploy on CI (Windows Desktop Release) hits ERR_PNPM_LOCKFILE_CONFIG_MISMATCH
# without legacy deploy; see pnpm warning on stage-desktop.
force-legacy-deploy=true
